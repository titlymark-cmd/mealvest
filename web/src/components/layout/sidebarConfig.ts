/**
 * Shared sizing for every persistent dashboard sidebar (admin, hotel
 * owner, …) — one fixed-width vertical sidebar on desktop, one
 * floating bottom tab bar below this breakpoint. Kept in one place so
 * every dashboard's shell behaves identically rather than drifting.
 */
export const SIDEBAR_WIDTH = 232;
export const SIDEBAR_MOBILE_BREAKPOINT = 768;
